#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main(){
	int A,B,C,D;
	scanf("%d%d%d%d",&A, &B, &C, &D);
	float resto;
	resto = A%2;
	if(B>C){
	if(D>A)
	if((C+D)>(A+B))
	if(C>0)
    if(D>0)
    printf("Valores aceitos");	
}
	else{
	    printf("Valores nao aceitos");
}
	return 0;
}

